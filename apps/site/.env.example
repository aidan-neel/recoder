# Neon connection string for the hosted waitlist (server-side only, never PUBLIC_).
# Create the table once with: psql "$DATABASE_URL" -f db/waitlist.sql
DATABASE_URL=postgresql://user:password@ep-example.us-east-2.aws.neon.tech/neondb?sslmode=require
